# Optional development overrides. Packaged builds do not embed these values;
# desktop users enter their own credentials during onboarding or in Settings.
EDGAR_IDENTITY="Your Name you@example.com"
FRED_API_KEY=""
# Create a free Tiingo account and supply your own token for explicit market refreshes.
# Starting or browsing the app does not call Tiingo.
TIINGO_API_TOKEN=""
DATABASE_PATH="finance_terminal.db"
FINANCE_TERMINAL_DATA_DIR=".finance-terminal"
# Optional; otherwise the signed-in Codex catalog default is used.
CODEX_MODEL=""
